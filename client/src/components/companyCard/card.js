import React from "React";



class Card extends React.Component {
    consructor(props) {
        super(props)

        this.state = {
            Company: props.Name,
            Symbol: props.Symbol
        };

        render() {
            return (
                
            )
        };

    }



}